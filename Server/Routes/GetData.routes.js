// TODO: Update this routes with the new Controller and Logic
/* import { Router } from "express";
import { getResumeUrl } from "../Controllers/GetExistingData.controllers.js";

const data = Router();

data.get("/resume", getResumeUrl);

export default data;
 */
